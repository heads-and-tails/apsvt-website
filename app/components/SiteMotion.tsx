"use client";
import { useEffect, useState } from "react";
import { usePathname } from "next/navigation";
export function SiteMotion(){const pathname=usePathname();const [done,setDone]=useState(false);const [progress,setProgress]=useState(0);useEffect(()=>{let value=0;const timer=setInterval(()=>{value=Math.min(100,value+Math.ceil(Math.random()*14));setProgress(value);if(value>=100){clearInterval(timer);setTimeout(()=>setDone(true),220)}},70);const scroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;document.documentElement.style.setProperty("--scroll",`${max?scrollY/max*100:0}%`)};addEventListener("scroll",scroll,{passive:true});return()=>{clearInterval(timer);removeEventListener("scroll",scroll)}},[]);if(pathname.startsWith("/panel"))return null;return <><div className={`loader ${done?"done":""}`} aria-hidden={done}><div className="loader-grid"/><div className="loader-orbit"><i/><i/><i/><b/></div><div className="loader-top mono"><span>APSVT / DIGITAL CAMPUS</span><span>KYIV · UKRAINE</span></div><strong>{String(progress).padStart(2,"0")}</strong><div className="loader-line"><span style={{width:`${progress}%`}}/></div></div><div className="scroll-progress"/></>}
