import { NextResponse } from "next/server";

type Context={params:Promise<{key:string[]}>};
export async function GET(request:Request,{params}:Context){try{const moduleName="cloudflare:workers";const {env}=await import(/* webpackIgnore: true */ moduleName);const {key}=await params;if(!env.MEDIA)return new NextResponse("Not found",{status:404});const requestHeaders=new Headers();const range=request.headers.get("range");if(range)requestHeaders.set("range",range);const object=await env.MEDIA.get(key.join("/"),range?{range:requestHeaders}:undefined);if(!object)return new NextResponse("Not found",{status:404});const headers=new Headers();object.writeHttpMetadata(headers);headers.set("etag",object.httpEtag);headers.set("accept-ranges","bytes");headers.set("cache-control",headers.get("cache-control")||"public, max-age=31536000, immutable");const responseRange=(object as {range?:{offset?:number;length?:number}}).range;if(range&&responseRange?.offset!==undefined&&responseRange.length!==undefined){headers.set("content-range",`bytes ${responseRange.offset}-${responseRange.offset+responseRange.length-1}/${object.size}`);headers.set("content-length",String(responseRange.length));return new NextResponse(object.body,{status:206,headers})}headers.set("content-length",String(object.size));return new NextResponse(object.body,{headers})}catch{return new NextResponse("Not found",{status:404})}}
